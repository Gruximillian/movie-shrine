import React from 'react';
import classes from "./Header.module.css";

const LoginModal = () => {
    return (
        <div className={`${classes.LoginModal} scale-transition scale-in`}>

        </div>
    );
};

export default LoginModal;
